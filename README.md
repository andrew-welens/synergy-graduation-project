# Система учета клиентов и заказов

Прототип веб-приложения для многопользовательского учета клиентов и заказов с ролевой моделью доступа, историей действий и базовыми отчетами.

**Дипломная работа**  
Тема: "Разработка многопользовательского веб-приложения для учета клиентов и заказов"  
Московский финансово-промышленный университет «Синергия»

## 📋 Описание проекта

Прототип веб-приложения, обеспечивающего многопользовательский доступ к функциям учета клиентов и заказов. Прототип предназначен для ежедневной работы сотрудников организации и обеспечивает:

- Управление клиентской базой с валидацией данных
- Создание и сопровождение заказов с историей статусов
- Каталог товаров и услуг
- Историю взаимодействий с клиентами
- Отчетность и аналитику
- Полный аудит действий пользователей
- Ролевую модель доступа (RBAC)

## 🛠 Технологический стек

### Backend
- **Node.js** — серверная платформа
- **Express** — веб-фреймворк
- **TypeScript** — типизация кода
- **Prisma ORM** — типобезопасная работа с БД
- **PostgreSQL** — реляционная база данных
- **JWT** — аутентификация
- **bcrypt** — хеширование паролей
- **class-validator** — валидация DTO
- **OpenAPI** — документация API
- **Jest** — тестирование

### Frontend
- **React** — UI библиотека (SPA)
- **TypeScript** — типизация
- **Vite** — сборщик
- **SASS** — стилизация
- **React Router** — маршрутизация
- **Zustand** — управление глобальным состоянием (аутентификация, уведомления)
- **XLSX** — экспорт отчетов в Excel
- **date-fns** — работа с датами

### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация сервисов

## ✨ Функциональность

### Реализованные модули

#### 1. Аутентификация и авторизация
- Вход по email/паролю
- JWT токены (access и refresh) с автоматическим обновлением
- Хранение токенов в httpOnly cookies для защиты от XSS
- Выход из системы
- Защита маршрутов по ролям

#### 2. Управление клиентами
- Создание, редактирование, удаление клиентов
- Поиск по имени, телефону, email
- Фильтрация по типу (юридическое/физическое лицо)
- Валидация телефона, email, ИНН
- Маски ввода для телефона и ИНН
- Проверка на дубликаты
- Привязка к менеджеру

#### 3. Управление заказами
- Создание заказов с позициями
- Редактирование состава заказа
- Смена статусов с историей переходов
- Назначение ответственного менеджера
- Автоматический расчет суммы
- Фильтрация по статусу, дате, менеджеру
- Отображение просроченных заказов
- История статусов с авторами изменений

#### 4. Каталог товаров и услуг
- Управление категориями
- Управление позициями (товары/услуги)
- Признак доступности позиции
- Цена и единица измерения
- Фильтрация по категории и доступности

#### 5. Взаимодействия с клиентами
- История контактов с клиентами
- Различные каналы взаимодействия
- Привязка к менеджеру
- Редактирование и удаление записей

#### 6. Отчеты
- Агрегаты по статусам заказов
- Отчеты по ответственным менеджерам
- Отчеты по дням
- Отчет по просроченным заказам
- Экспорт в Excel
- Фильтрация по датам

#### 7. Аудит действий
- Журнал всех действий пользователей
- Фильтрация по сущности, пользователю, дате
- Типы действий (создание, обновление, удаление, смена статуса)
- Просмотр для администратора и аналитика

#### 8. Управление пользователями
- CRUD операции с пользователями
- Управление ролями
- Активация/деактивация пользователей
- Доступ только для администратора

#### 9. Профиль пользователя
- Просмотр информации о текущем пользователе

### Роли и права доступа

| Роль | Описание | Права |
|------|----------|-------|
| **Администратор** | Полный доступ | Все операции, управление пользователями |
| **Менеджер** | Управление заказами и клиентами | CRUD клиентов и заказов, смена статусов, каталог, отчеты, аудит |
| **Оператор** | Работа с клиентами и заказами | CRUD клиентов и заказов, ограниченная смена статусов, просмотр каталога |
| **Аналитик** | Просмотр данных и отчетов | Только чтение: клиенты, заказы, отчеты, аудит |

### Статусы заказов

- **new** — Новый заказ
- **in_progress** — В обработке
- **done** — Выполнен
- **canceled** — Отменен

Переходы между статусами контролируются бизнес-логикой и правами пользователя.

## 🚀 Установка и запуск

### Требования

- Node.js
- PostgreSQL (или Docker)
- npm или yarn

### Локальная разработка

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd synergy-graduation-project
```

#### 2. Настройка Backend

```bash
cd server
npm install

# Создайте .env файл
cp .env.example .env

# Настройте DATABASE_URL в .env
# DATABASE_URL="postgresql://user:password@localhost:5432/synergy"

# Генерация Prisma клиента
npx prisma generate

# Применение миграций
npx prisma db push

# Заполнение начальными данными
npm run prisma:seed

# Запуск сервера
npm run dev
```

Сервер запустится на `http://localhost:3000`
API документация: `http://localhost:3000/api/docs`

#### 3. Настройка Frontend

```bash
cd client
npm install
npm run dev
```

Приложение откроется на `http://localhost:5173`

### Docker развертывание

#### Быстрый старт

```bash
# Создайте .env файл в корне проекта
cp .env.example .env

# Отредактируйте .env, особенно JWT_SECRET для production

# Запуск всех сервисов
docker-compose up -d

# Инициализация БД (после первого запуска)
docker-compose exec server npx prisma migrate deploy
docker-compose exec server npm run prisma:seed
```

Доступ:
- Клиент: `http://localhost`
- API: `http://localhost:3000/api`
- API Docs: `http://localhost:3000/api/docs`

#### Разработка с Docker

Для разработки используйте только PostgreSQL из Docker:

```bash
docker-compose -f docker-compose.dev.yml up -d postgres
```

Затем запускайте клиент и сервер локально:

```bash
# Терминал 1
cd client && npm run dev

# Терминал 2
cd server && npm run dev
```


## 📁 Архитектура проекта

### Общая архитектура

Система построена по принципу модульного монолита с четким разделением слоев:

- **React SPA** (клиент) → **Express API** (сервер) → **Prisma ORM** → **PostgreSQL** (БД)

**Слои сервера:**
- **Controllers** — обработка HTTP запросов, валидация входных данных
- **Services** — бизнес-логика, правила работы с данными
- **Prisma ORM** — типобезопасный доступ к базе данных
- **PostgreSQL** — хранение данных

Такая архитектура упрощает развертывание и позволяет при необходимости выделить модули в микросервисы.

### Структура Backend

```
server/
├── src/
│   ├── controllers/        # HTTP обработчики запросов
│   │   ├── auth/           # Аутентификация
│   │   ├── users/          # Управление пользователями
│   │   ├── clients/        # Клиенты
│   │   ├── orders/         # Заказы
│   │   ├── catalog/        # Каталог
│   │   ├── interactions/   # Взаимодействия
│   │   ├── reports/        # Отчеты
│   │   ├── audit/          # Аудит
│   │   └── common/         # Общие DTO
│   ├── services/           # Бизнес-логика
│   │   ├── auth/           # Аутентификация
│   │   ├── users/          # Управление пользователями
│   │   ├── clients/        # Клиенты
│   │   ├── orders/         # Заказы
│   │   ├── catalog/        # Каталог
│   │   ├── interactions/   # Взаимодействия
│   │   ├── reports/        # Отчеты
│   │   ├── audit/          # Аудит
│   │   ├── bootstrap/      # Инициализация системы
│   │   ├── prisma/         # Prisma сервис
│   │   ├── common/         # Общие сервисы (ошибки)
│   │   ├── data/           # Хранилище данных
│   │   ├── types/          # Типы и модели
│   │   └── utils/          # Утилиты (JWT, response)
│   ├── middleware/         # Middleware
│   │   ├── auth.ts         # JWT аутентификация
│   │   ├── permissions.ts  # Проверка прав доступа
│   │   ├── validate.ts     # Валидация DTO
│   │   ├── error-handler.ts # Обработка ошибок
│   │   └── async-handler.ts # Обработка асинхронных ошибок
│   ├── routes/             # Регистрация маршрутов
│   ├── config/             # Конфигурация (OpenAPI)
│   └── main.ts             # Точка входа
├── prisma/
│   ├── schema.prisma       # Схема БД
│   └── seed.ts             # Начальные данные
├── test/                   # Тесты
│   ├── *.e2e-spec.ts       # E2E тесты
│   └── *.spec.ts           # Unit тесты
└── package.json
```

#### Описание слоев Backend

**Controllers** (`src/controllers/`)
- Обрабатывают HTTP запросы и ответы
- Валидируют входные данные через DTO
- Вызывают соответствующие сервисы для выполнения бизнес-логики
- Формируют HTTP ответы с правильными статус-кодами
- Каждый модуль имеет свой контроллер (auth, users, clients, orders и т.д.)

**Services** (`src/services/`)
- Содержат всю бизнес-логику приложения
- Реализуют правила работы с данными (валидация, проверки, вычисления)
- Выполняют операции с базой данных через Prisma
- Обеспечивают транзакционность критичных операций
- Логируют действия в аудит через AuditService
- Каждый модуль имеет свой сервис, соответствующий контроллеру
- `prisma/` — централизованный сервис для работы с Prisma клиентом
- `bootstrap/` — инициализация системы при старте
- `common/` — общие сервисы (обработка ошибок)
- `data/` — хранилище данных
- `types/` — типы TypeScript и модели данных
- `utils/` — утилиты (JWT, формирование ответов)

**Middleware** (`src/middleware/`)
- `auth.ts` — проверяет JWT токены, извлекает информацию о пользователе
- `permissions.ts` — проверяет права доступа на основе роли пользователя (RBAC)
- `validate.ts` — валидирует DTO через class-validator перед обработкой запроса
- `error-handler.ts` — централизованная обработка ошибок, формирование унифицированных ответов
- `async-handler.ts` — обертка для асинхронных контроллеров, автоматический перехват ошибок

**Routes** (`src/routes/`)
- Регистрирует все маршруты API
- Применяет middleware для аутентификации и проверки прав
- Связывает HTTP методы с контроллерами

### Структура Frontend

```
client/
├── src/
│   ├── pages/              # Страницы приложения
│   │   ├── login.tsx       # Вход
│   │   ├── dashboard.tsx   # Главная
│   │   ├── clients.tsx     # Список клиентов
│   │   ├── client-detail.tsx # Карточка клиента
│   │   ├── orders.tsx      # Список заказов
│   │   ├── order-detail.tsx # Карточка заказа
│   │   ├── catalog.tsx     # Каталог
│   │   ├── interactions.tsx # Взаимодействия
│   │   ├── reports.tsx     # Отчеты
│   │   ├── audit.tsx        # Аудит
│   │   ├── users.tsx        # Пользователи
│   │   └── profile.tsx      # Профиль
│   ├── components/         # Переиспользуемые компоненты
│   │   ├── layout.tsx       # Основной layout
│   │   ├── protected.tsx   # Защита маршрутов
│   │   ├── pagination.tsx   # Пагинация
│   │   ├── date-picker.tsx  # Выбор даты
│   │   ├── confirm-dialog.tsx # Диалог подтверждения
│   │   └── ...
│   ├── services/           # API клиенты
│   │   ├── http.ts         # HTTP клиент
│   │   ├── auth.ts         # Аутентификация
│   │   ├── clients.ts      # Клиенты
│   │   ├── orders.ts       # Заказы
│   │   ├── catalog.ts      # Каталог
│   │   ├── interactions.ts # Взаимодействия
│   │   ├── reports.ts      # Отчеты
│   │   ├── audit.ts        # Аудит
│   │   ├── users.ts        # Пользователи
│   │   └── types.ts        # Типы данных
│   ├── hooks/              # Пользовательские хуки
│   │   ├── use-debounce.ts
│   │   ├── use-keyboard-shortcuts.ts
│   │   └── use-min-loading.ts
│   ├── utils/              # Утилиты
│   │   ├── auth.ts         # Работа с токенами
│   │   ├── toast.ts        # Уведомления
│   │   └── clipboard.ts    # Работа с буфером
│   ├── styles/             # Глобальные стили
│   │   ├── global.scss
│   │   └── animations.scss
│   ├── App.tsx             # Маршруты
│   └── main.tsx            # Точка входа
├── public/                 # Статические файлы
└── package.json
```

## 🔌 API Endpoints

### Аутентификация

- `POST /api/auth/login` — вход в систему (токены устанавливаются в httpOnly cookies)
- `POST /api/auth/refresh` — обновление access токена (refresh токен из cookie или body)
- `POST /api/auth/logout` — выход (очистка cookies)

### Клиенты

- `GET /api/clients` — список клиентов (с фильтрами и пагинацией)
- `GET /api/clients/:id` — карточка клиента
- `POST /api/clients` — создание клиента
- `PUT /api/clients/:id` — обновление клиента
- `DELETE /api/clients/:id` — удаление клиента

### Заказы

- `GET /api/orders` — список заказов (с фильтрами и пагинацией)
- `GET /api/orders/:id` — карточка заказа
- `POST /api/orders` — создание заказа
- `PUT /api/orders/:id` — обновление заказа
- `POST /api/orders/:id/status` — смена статуса заказа

### Каталог

- `GET /api/categories` — список категорий
- `POST /api/categories` — создание категории
- `PUT /api/categories/:id` — обновление категории
- `GET /api/products` — список товаров/услуг
- `POST /api/products` — создание товара/услуги
- `PUT /api/products/:id` — обновление товара/услуги

### Взаимодействия

- `GET /api/clients/:id/interactions` — список взаимодействий клиента
- `POST /api/clients/:id/interactions` — добавление взаимодействия
- `PUT /api/interactions/:id` — обновление взаимодействия
- `DELETE /api/interactions/:id` — удаление взаимодействия

### Отчеты

- `GET /api/reports/orders` — отчет по заказам
- `GET /api/reports/overdue` — отчет по просроченным заказам

### Аудит

- `GET /api/audit` — журнал аудита (с фильтрами)

### Пользователи

- `GET /api/users` — список пользователей
- `GET /api/users/:id` — карточка пользователя
- `POST /api/users` — создание пользователя
- `PUT /api/users/:id` — обновление пользователя
- `DELETE /api/users/:id` — удаление пользователя

Полная документация API доступна по адресу `/api/docs` после запуска сервера.

## 🔒 Безопасность

### Реализованные меры

- **JWT аутентификация** с refresh токенами
- **Хранение токенов в httpOnly cookies** — защита от XSS атак
- **httpOnly cookies с sameSite политикой** — защита от CSRF
- **Хеширование паролей** с использованием bcrypt (10 раундов)
- **RBAC (Role-Based Access Control)** — проверка прав на сервере при каждом запросе
- **Rate limiting** — защита от брутфорса (100 запросов/минуту на общие endpoints, 5 попыток/минуту на вход)
- **Валидация всех входных данных** на клиенте и сервере
- **Защита от XSS** — React автоматически экранирует данные
- **CORS** настройки для контроля доступа
- **Транзакции** для критичных операций (создание заказа, смена статуса, обновление заказа) — обеспечивают атомарность и целостность данных через Prisma `$transaction`
- **Аудит действий** — логирование всех операций с указанием пользователя
- **UUID вместо числовых ID** — защита от перебора
- **Нормализация данных** (телефон, email)
- **Проверка на дубликаты** при создании клиентов
- **Валидация переходов** между статусами заказов

## 🧪 Тестирование

### Запуск тестов

```bash
cd server

# Все тесты
npm test

# Только unit тесты
npm run test:unit

# Только E2E тесты
npm run test:e2e

# Тесты с покрытием
npm run test:coverage
```

### Покрытие тестами

Проект использует Jest с настройкой минимального покрытия:
- **Branches**: 70%
- **Functions**: 70%
- **Lines**: 70%
- **Statements**: 70%

Типы тестирования:
- Функциональное тестирование
- Приемочное тестирование
- Тестирование безопасности
- Нагрузочное тестирование

Отчет о покрытии генерируется в форматах: text, lcov, html (в директории `coverage/`)

## 📊 Производительность

### Результаты нагрузочного тестирования

При нагрузке до 50 одновременных пользователей:
- `GET /api/clients`: p95 ≤ 900 мс
- `GET /api/orders`: p95 ≤ 1000 мс
- `POST /api/orders`: p95 ≤ 1200 мс
- `POST /api/orders/:id/status`: p95 ≤ 1100 мс

### Реализованные оптимизации

- **Индексация БД**: составные индексы для оптимизации запросов
  - `Order`: `[status, createdAt]`, `[managerId, status]`
  - `Client`: `[managerId]`, `[type]`, `[createdAt]`
  - `AuditLog`: `[userId, createdAt]`, `[entityType, createdAt]`, `[createdAt]`
  - `Interaction`: `[clientId, createdAt]`, `[managerId]`
- **Транзакции**: все критичные операции (создание заказов, смена статусов, обновление заказов) выполняются в транзакциях для обеспечения целостности данных
- **Строгая пагинация**: ограничение pageSize для предотвращения перегрузки
- **Оптимизация запросов**: использование `Promise.all` для параллельных запросов, селективные поля через `select`

### Рекомендации по дальнейшей оптимизации

- Кэширование справочников каталога
- Асинхронная обработка уведомлений
- Connection pooling для БД
- Кэширование на уровне Redis для часто запрашиваемых данных

## 🗄 База данных

### Основные сущности

- **User** — пользователи системы
- **Client** — клиенты
- **Order** — заказы
- **OrderItem** — позиции заказа
- **OrderStatusHistory** — история статусов
- **Category** — категории каталога
- **Product** — товары/услуги
- **Interaction** — взаимодействия с клиентами
- **AuditLog** — журнал аудита

### Миграции

```bash
# Создание новой миграции
npx prisma migrate dev --name migration_name

# Применение миграций в production
npx prisma migrate deploy

# Просмотр БД в Prisma Studio
npx prisma studio
```

## 🚢 Развертывание

### Переменные окружения

**Backend (.env в server/)**
```
DATABASE_URL=postgresql://user:password@localhost:5432/synergy
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d
PORT=3000
```

**Docker (.env в корне)**
```
POSTGRES_USER=synergy
POSTGRES_PASSWORD=synergy
POSTGRES_DB=synergy
POSTGRES_PORT=5432
SERVER_PORT=3000
CLIENT_PORT=80
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d
```

## 📈 Перспективы развития

### Планируемые улучшения

- Расширение отчетности и аналитики (дашборды, KPI)
- Интеграции с внешними системами (email, CRM/ERP)
- Внедрение 2FA для усиления безопасности
- Оптимизация производительности (кэширование, индексы)
- Переход к микросервисной архитектуре при росте нагрузки
- Мобильное приложение
- Уведомления в реальном времени
- Расширенная аналитика и BI

